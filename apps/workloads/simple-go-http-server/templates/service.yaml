apiVersion: v1
kind: Service
metadata:
  name: {{ include "simple-go-http-server.fullname" . }}
  labels:
    {{- include "simple-go-http-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.containerPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "simple-go-http-server.selectorLabels" . | nindent 4 }}
