# GENERATED - DO NOT EDIT
# Source: apps/infra/cert-manager/app-config.yaml
# Run 'go run scripts/generate-configs.go' to regenerate
#
# ProjectConfig enables auto-promotion for all stages except test.
# test stage requires manual promotion to initiate the pipeline.
apiVersion: kargo.akuity.io/v1alpha1
kind: ProjectConfig
metadata:
  name: cert-manager
  namespace: cert-manager
spec:
  promotionPolicies:
    - stageSelector:
        name: dev
      autoPromotionEnabled: true
    - stageSelector:
        name: staging
      autoPromotionEnabled: true
    - stageSelector:
        name: infra
      autoPromotionEnabled: true
    - stageSelector:
        name: prod-au
      autoPromotionEnabled: true
    - stageSelector:
        name: prod-eu
      autoPromotionEnabled: true
    - stageSelector:
        name: prod-us
      autoPromotionEnabled: true
